// ApRez brand colors — sourced from packages/shared/theme/colors.js so web + mobile stay in sync.
// Metro handles the CommonJS interop transparently.
const { Colors } = require('../../../../packages/shared/theme/colors');

export { Colors };
